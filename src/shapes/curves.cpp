#include <lightwave.hpp>

#include "curves.hpp"

// This file only exists to prevent some problem with the registry and including .cpp files into other .cpp files, move on :>

REGISTER_SHAPE(Curves, "curve")
